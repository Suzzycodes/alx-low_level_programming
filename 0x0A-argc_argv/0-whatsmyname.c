#include "main.h"
#include <stdio.h>
/**
 * main - my main fuction
 * @argc: first argument
 * @argv: second argument
 * Return: always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
