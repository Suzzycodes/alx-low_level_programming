#include "main.h"
/**
 * print_line - print a long line
 * @n: number of times to print n
 * Return: alwsys 0
 */

void print_line(int n)
{
	char _;

	for (n = 0 ; n >= 8 ; n++)
	{
		_putchar(_);
	if (n >= 0)
	continue;
	else
		_putchar('\n');
	}
}
